# Experiments Log

One row per experiment. Most recent at the top. **Don't delete rows** — even failed/superseded experiments are evidence. Mark status in the Result column.

Status legend: ✅ confirmed hypothesis | ❌ falsified hypothesis | ⚠️ partial / mixed | 🔁 superseded by later exp | 🛑 invalid (bug, not science)

| # | File | Hypothesis (one sentence) | Tier | Result | Key number | Learning → next |
|---|---|---|---|---|---|---|
| 74 | `exp74_brain_recovery.py` | Sweep adjacency-recovery estimators on voxel timeseries from the exp23 TRIBE stub: lag-1 cross-correlation (exp23 baseline), lag-K cross-correlation, and Granger linear regression (V[t+1]=V[t]@W with ridge). Plus a corruption sweep — flip a fraction f of cells in A_s, measure F1 — to characterize how much recovery error TL closure tolerates independent of method. Hypothesis from exp23: better estimators clear a Jaccard bar that lag1 misses, raising TL F1 on A_hat closer to TL on A_s. Also catch a possible exp23 metric problem: was F1=0.347 vs F1=1.000 on A_hat vs A_s actually a recovery-quality gap, or just a closure-density saturation artifact? | T1 (laptop, ~3 min) | ⚠️ partial — refines exp23, exposes a metric saturation bug, falsifies "Granger fixes recovery" at the diagnostic regime | At brain p=0.08 (exp23 setup) **closure density = 0.984, chance F1 = 0.992** — predict-all-ones nearly maxes out F1, so exp23's "TL on A_s F1=1.000" was mostly the trivial baseline. **Re-run at brain p=0.025 (closure density 0.122, chance F1 = 0.217)**: TL on A_s F1=**1.000**, lag1 F1=**0.091**, lagK F1=0.090, **Granger F1=0.183** — *all three recovery methods fail*, Granger comes closest but is still below the predict-all-ones chance baseline. Recovery Jaccard vs A_s: lag1=0.085, lagK=0.075, Granger=0.148. **Corruption sweep on A_s itself**: F1 stays at 0.994-1.000 even at 40% random flips (Jaccard 0.10) — random edge perturbations preserve closure structure essentially perfectly at every density tested (0.025, 0.04, 0.08). | Two findings, opposing flavors. (1) **exp23's headline numbers were partly metric-saturated**: F1=1.000 on A_s at p=0.08 is mostly the predict-all-ones baseline, not a TL achievement. The right way to evaluate "TL on TRIBE" is at sparse closure densities where F1 isn't trivially high. **At the diagnostic regime (p=0.025), bottom-up recovery uniformly fails** — Granger, the strongest of three standard estimators, gets F1=0.183 < chance baseline 0.217. The "TL applies to TRIBE outputs" claim is **not currently supported** for sparse brains. (2) **The corruption sweep flatness is itself a finding**: TL closure is structurally robust to random edge perturbations at every density tested — random recovery noise doesn't hurt. The recovery methods fail because they produce **systematically biased** A_hat (too-sparse closures, wrong component structure), not because they produce randomly wrong A_hat. Together these argue: **the recovery problem is the real research question for "TL on TRIBE"**, and crude bottom-up estimators (cross-correlation, Granger) don't solve it. Next: (a) **end-to-end joint optimization** — treat A_hat as a continuous latent, train it together with (α, β, γ) against held-out reachability supervision; this is the right shape, since closure F1 is what we care about, not Jaccard; (b) **partial correlation / Graphical Lasso** for symmetric structure + lag1 for orientation, untested here; (c) **structured-prior recovery** — assume modular/small-world brain connectivity and fit a generative graph model whose closure matches observed voxel co-activations; (d) before more recovery work, **re-do exp23's claim at p=0.025 to get an honest baseline number**, since the original p=0.08 result is now metric-saturated. |
| 73 | `exp73_tribe_closure.py` | Apply the 3-scalar TL closure recurrence (exp44/47/53/54) to a brain-connectivity graph derived from TRIBE-style voxel timeseries, as the natural follow-on to Meta's TRIBE v2 release. Synthetic stub stands in for real TRIBE weights: latent K=64 directed brain A* → per-subject perturbations A_s → simulated voxel timeseries V[stim, t, K] from a saturating linear dynamical system → A_hat recovered via lag-1 cross-correlation + top-k thresholding. Train TL on synthetic K=16 DAGs, evaluate zero-shot on (a) random in-dist DAGs, (b) ground-truth A_s, (c) recovered A_hat (clean), (d) recovered A_hat (voxel noise σ=0.20). Hypothesis: TL closure works on A_s (replicates exp44 at K=64); the open question is whether crude A_hat recovery is good enough that TL closure on A_hat matches closure on A_s. | T1 (laptop, ~2 min) | ⚠️ partial — TL works on the connectivity graph, but adjacency recovery from voxel timeseries is the bottleneck | **TL on ground-truth brain A_s: F1=1.000 ± 0.000** (3 params, K=64, K_iters=10) — replicates exp47's scale-invariance claim on a directed graph with 304 edges. **TL on recovered A_hat (clean voxels): F1=0.347 ± 0.000** — collapses. **TL on A_hat with σ=0.20 noise: F1=0.279**. Adjacency recovery Jaccard vs ground truth: **0.176 clean, 0.086 noisy**. In-dist TL F1=0.998 ± 0.003; MLP (37k params) in-dist F1=0.506 ± 0.021. Trained TL converges to α≈4.7, β≈7.7, γ≈-4.3 (close to exp44 operating point). | TL is not the bottleneck for brain-graph closure — adjacency recovery is. Given clean A_s, the same 3-scalar recurrence that handles real Python imports zero-shot also handles K=64 directed brain graphs zero-shot at F1=1.000. The collapse to F1=0.347 on A_hat tracks one-to-one with the recovery Jaccard (0.176): garbage adjacency in, garbage closure out. This **inverts the natural assumption** that the hard part of "TL on TRIBE outputs" is the TL part — the hard part is the upstream feature extraction (lag-1 cross-correlation is too crude). Combined with exp44/47/53/54: TL is robustly the right primitive whenever you can hand it a clean directed graph; the research direction this exposes is **adjacency recovery from neural timeseries**, not TL-architecture surgery. Next: (a) replace top-k cross-correlation with a real TRIBE v2 forward pass — tribe_stub_voxels(...) is already isolated as a one-line swap; (b) try better recovery estimators (partial correlation, sparse inverse covariance / Graphical Lasso, lag-K Granger) — the gain matters far more than further TL-side tuning; (c) test if jointly optimizing A_hat and (α, β, γ) end-to-end against held-out reachability supervision recovers a usable graph at higher Jaccard; (d) honest scope statement: "TL applies to TRIBE outputs IF you can recover adjacency at Jaccard ≥ 0.5" — quantify the recovery threshold needed for F1>0.9. |
| 72 | `exp72_characterize_tail_sets.py` | Characterize the tail-extremal primitive sets exp71's TL search recovers. Three structural hypotheses: H_band (concentrated in [x,2x)), H_SPF (smallest-prime-factor > √x), H_ext (the search returns the maximal primitive extension max-ext([x,2x)) := [x,2x) ∪ {n in [2x, N] : no a in [x,2x) divides n}). Inspect at x ∈ {10, 100, 1000}: dyadic-bucket coverage, SPF profile, set agreement against primes_tail / [x,2x) / SPF>√x / max-ext. | T1 (laptop, ~1 min for 3 search runs + analysis) | ✅ refined into a sharper characterization than any of H_band/H_SPF/H_ext alone | At **x=1000** search exactly equals max-ext([1000,2000)) — 1001/1001 elements match, f=0.0957 identical, both primitive. At **x=100** search ⊂ max-ext (533/995 elements, only-search=0, only-ext=462) and max-ext is **not primitive** — search f=0.2507 vs max-ext f=0.3120. Same pattern at **x=10** (313/1067, only-ext=754, max-ext non-primitive). At x=10 search includes all of [10,20) plus 21,23,25,27,29,…, dropping {22,24,26,28} because 11,12,13,14 ∈ search and divide them. Search neither reduces to the dyadic band nor to "SPF > √x" (which is non-primitive at small x). | The TL search recovers the **largest primitive subset** of max-ext([x,2x)). The seed [x,2x) is automatically primitive (b/a < 2 ⟹ a∤b); max-ext extends by the "no [x,2x)-element divides me" filter, which is necessary but not sufficient for primitivity (e.g., 201∤any [100,200)-element, 603∤any [100,200)-element, but 201\|603, so both ∈ max-ext but not both ∈ any primitive subset). When 2x ≥ N/2, max-ext degenerates to the band itself and IS primitive — search recovers it exactly. This identifies the candidate primitive-set family that drives the tail-bound regime: not primes, not pure dyadic bands, but "primitive subsets of the (band, ¬band-divisible)-construction". For the GPT-5.4 Pro proof's sub-Markov chain, the relevant high-mass primitive sets in each tail are this family — exp60 should build the chain's transition tensor with these sets as the absorbing class, not primes. The fact that gradient descent on a TL einsum naturally finds these sets is the main "vibe-math" finding here: the search rediscovers a non-obvious primitive-set family without being told its construction. |
| 71 | `exp71_tail_bound.py` | Direct test of the tail-restricted GPT-5.4 Pro bound: ∑_{a in A, a>x} 1/(a log a) ≤ 1 + C/log x for primitive A. For each x in {2,5,10,25,50,100,250,500,1000}, restrict TL search to indices [x..N=2000] (head pinned to s≈0), run cold-start lambda anneal (1e-3→1e2, 3000 steps), greedy-reduce. Compare f(A_search) to f(primes ∩ [x, N]) and to the ceiling 1 + 1/log x. Two predictions: (i) bound holds (f_best ≤ 1 + C/log x with bounded C); (ii) primes are extremal in the *tail* problem too. | T1 (laptop, ~3 min for 9 runs) | ✅ bound holds with enormous slack; ❌ second prediction — primes are NOT tail-extremal | At x=2 search recovers primes exactly (f=1.5053, \|A\|=303). **For x ≥ 5 the search finds primitive sets strictly beating primes-in-tail**: x=10 → f=0.5059 vs f(primes∩[10,N])=0.2829 (1.79× richer); x=50 → 0.3047 vs 0.1171 (2.6×); x=100 → 0.2507 vs 0.0838 (3.0×); x=1000 → 0.0957 vs 0.0130 (7.4×). All recovered sets primitive. Largest C_emp = (f_best − 1)·log x over x ∈ [5, 1000] is −0.51 (negative = far below the bound). Gap between f_best and ceiling 1+1/log x stays ~0.93–1.05 throughout. | (1) The GPT-5.4 Pro bound is comfortably non-tight at N=2000 — search-found tail primitives stay well below 1, so the leading constant in the ceiling is not approached. The 1+O(1/log x) form is what the *worst possible* primitive set could conceivably hit; observed sets are far from that frontier. (2) **Primes lose extremality the moment we restrict to a tail**, by 2-7×. Likely explanation: integers in dyadic-style bands [x, 2x) are automatically primitive (a, b in [x, 2x), a < b ⟹ b/a < 2 ⟹ b not a multiple of a), and they accumulate more 1/(n log n) mass than the sparse prime-tail. The tail search probably discovers approximately-band-structured primitive sets. Next: (a) **exp72** — characterize the tail-extremal sets the search returns: are they dyadic-band primitives? Have specific smallest-prime-factor signatures? Do they match the sub-Markov-chain visit-distribution structure of the GPT-5.4 Pro proof? (b) sub-Markov-chain forward construction in TL: build the actual chain with transitions ∝ divisibility and visit mass ∝ 1/(n log n), compute first-hit mass on primitive sets via einsum recurrence, verify Lichtman-style identities. (c) push N higher (sparse D representation needed) to test asymptotic tightness — at N=2000 the constant in O(1/log x) is huge; is there a regime where the ceiling matters? |
| 70 | `exp70_norm_aware_primitive.py` | The single-tensor TL primitive-set search (exp69 PROBE A) is an indicator-vector role per the Bernstein-Newhouse framing (arXiv:2409.20325, "Old Optimizer, New Norm"). Coordinate-wise normalized updates (Adam, signSGD) should match the geometry; un-normalized SGD and whole-tensor 2-norm steepest descent (NormSGD) should not. Compare four updaters on identical setup (cold-start, lambda anneal 1e-3→1e2, 3000 steps, N=2000) and report final f(s), v(s), primitivity of rounded set, and L1 distance to the prime indicator. | T1 (laptop, ~80s for 4 runs) | ✅ confirms Bernstein-Newhouse prescription on this problem | Adam (lr=5e-2): f=1.505, v=0.0001, primitive=True, **L1 to primes = 0.07** (essentially exact prime indicator). signSGD (lr=5e-2): f_soft=1.873 oversaturated, but greedy reduction → exactly f(primes)=1.5053, L1=4. NormSGD (lr=5): stuck at f=1.42, L1=58. Plain SGD (lr=0.5): collapses to f=0.007, L1=278. | Coordinate-wise normalization is the right norm for this tensor role; whole-vector normalization isn't, and unnormalized SGD fails entirely. The signal-strength range across coordinates (w[a]=1/(a log a) varies ~4 orders of magnitude over [2..2001]) makes per-coordinate adaptive scaling necessary. The Bernstein-Newhouse argument is concretely visible in a math-search setting: indicator-vector role wants Linf-style geometry (signSGD / Adam), not L2 (NormSGD). Implication for scaling: when TL math-search adds more learnable tensors (relation matrices D, lambda multipliers, learnable weight functions w), each role should get its own update rule from the paper's lookup table — Muon/Shampoo for relation matrices, Adam-class for indicator vectors. |
| 69 | `exp69_tl_primitive_search.py` | Two probes of the GPT-5.4 Pro Erdős #1196 bound via TL+autograd. PROBE A: cold-start theta with lambda annealed log-linearly 1e-3→1e2, see whether gradient descent rediscovers the primes from scratch. PROBE B (falsification): warm-start theta near indicator(primes_N), apply -f loss with no constraint penalty, then greedy-primitive-reduce the rounded set — does gradient flow find a primitive subset with f exceeding f(primes)? Per the bound, no exceedance should be possible; per conjectured tightness at primes, the search should converge there. | T1 (laptop, ~20s) | ✅ both probes consistent with the bound | PROBE A: cold-start + anneal converges to f=1.5053, \|S\|=303, v=0.0001 — exactly the prime indicator at N=2000 (303 primes, f(primes)=1.505349). PROBE B: warm-start drifts to f_soft=2.5458 with non-primitive \|S\|=454, but greedy reduction collapses **back to exactly f(primes)=1.5053** (Δ=+0.000000) — no exceedance found. | Empirical confirmation that at N=2000 the primes are extremal among primitive sets reachable by TL+autograd. PROBE A's lambda annealing is the operationally interesting bit: naive single-lambda search collapses (quadratic v(s) at uniform s=0.5 dominates the small w[a] gradients), but log-linear annealing gives the f-signal time to accumulate before the constraint sharpens. This is the same trick the GPT-5.4 Pro proof uses — gradually concentrating visit mass via the divisibility structure rather than enforcing primitivity all at once. Next: (a) tail-supported version (theta on [x..N] only) to test 1+O(1/log x) directly; (b) falsification probe that varies the visit distribution (not just 1/(n log n)) to see whether that weight is special. |
| 68 | `exp68_erdos_primitive_sum.py` | Erdős's primitive-set machinery (divisibility relation, "is-primitive" predicate, Erdős sum f(A)=∑1/(a log a)) lifts cleanly into TL: D[a,b]=1[a divides b, a≠b], primitive predicate = einsum("a,ab,b->", S, D, S), f(S) = einsum("a,a->", S, w). The April 2026 GPT-5.4 Pro proof of Erdős #1196 (∑_{a∈A,a>x} 1/(a log a) ≤ 1+O(1/log x)) uses a sub-Markov chain whose visit mass is itself an einsum recurrence — so the TL substrate is the right shape for the problem. Verify: (1) einsum primitive-predicate matches direct check on canonical sets at N=2000; (2) f(primes_2..M) climbs toward the Erdős-Sárközy-Szemerédi constant ~1.6366 as M grows; (3) f(primes) tops every other primitive set we can write down up to M=10⁴. | T1 (laptop, ~15s) | ✅ all three checks pass | (1) all 6 cases match (primes, powers-of-2, semiprimes p·q, {2,4,6}, {3,9,27}, {6,10,15}); (2) f(primes): M=10³→1.4923, 10⁴→1.5282, 10⁵→1.5498, 10⁶→1.5642 (target ≈1.6366); (3) at M=10⁴ primes top all candidates: f(primes)=1.5282 vs squarefree-odd primitive 0.8068, semiprimes 0.7761, primes-tail [100,M] 0.1066, primes-tail [1000,M] 0.0358. | TL substrate is consistent with classical NT computation. Foundation for exp69 (differentiable falsification) and exp70 (optimizer comparison). The connection to GPT-5.4 Pro's proof: their sub-Markov chain has visit mass π[n]∝1/(n log n) and divisibility-encoding transitions — both are einsum recurrences over (D, w) and could be ported into TL form, making the constants in 1+O(1/log x) explicitly searchable by gradient descent rather than constructed by hand. |
| 63 | `exp63_sparse_closure.py` | exp59A found dense TL closure OOMs at Hanoi N=10 (~13 GB). Drop the dense V×V representation: implement BFS-per-source (build closure as Python sets, no V×V matrix) and BFS-per-query (single-pair reachability, no closure materialized). Compare on Hanoi state graphs and on a synthetic 10k-entity / 50k-fact "openhuman-scale" KB. Predict BFS-per-query is the right hot-path pick at openhuman scale. | T1 (laptop, ~2 min) | ✅ confirmed — BFS-per-query at 10k entities runs at 1.5 ms/query | **Hanoi state graphs** (V, E, dense_s, bfs_src_s, bfs_q_ms): N=3 (27, 78, 0.00, 0.00, 0.005); N=5 (243, 726, 0.01, 0.01, 0.03); N=7 (2187, 6558, 2.91, **1.29**, 0.28); N=8 (6561, 19680, 57.92, **16.06**, 0.83). BFS-per-source 2.3-3.6× faster than dense at N=7-8. **Openhuman-scale** (10k entities, ~50k edges, forest backbone + random cross-edges): BFS-per-query **1.474 ms/query** (98.4% reachable in this dense random graph), full dense closure tensor would be 381 MB, full BFS-per-source closure runs in 79 s producing 99M cells. **First draft burned 40 min on a `torch.sparse` fixpoint that densified inside the loop** (PyTorch sparse@sparse matmul is not first-class). Killed and rewritten with proper graph algorithms. | The exp59A memory wall is a *representation* choice, not a TL-fundamental limit. Three substrate options now characterized: (a) **dense closure** (BLAS-fast per cell, O(V²) memory — best for batched all-pairs at small V); (b) **BFS-per-source** (O(closure-cells) memory, O(V(V+E)) time — best for full closure when graph is sparse-ish); (c) **BFS-per-query** (O(V+E) per query, no closure stored — best for openhuman's hot path of one query per user interaction). For OPENHUMAN_TL_MEMO's "10k+ entity KB" claim: hot path = BFS-per-query at <10 ms, cold path = optional dense precompute. The "use existing graph algorithms, not a sparse storage layer" lesson applies generally — when "sparse" is on the critical path, the algorithm has to genuinely avoid the V×V intermediate, not just store it sparsely. |
| 67 | `exp67_provenance.py` | OPENHUMAN_TL_MEMO §3 ("auditability by construction") claims every TL inference leaves a rule chain. Operationalize: alongside the boolean answer, return the full derivation tree (proof trees as data — provenance semirings à la Green/Karvounarakis/Tannen 2007). For each query, recursively gather every chain of (primitive facts, rule firings) that supports it. Test with grandparent / uncle / cousin and a counterfactual (retract one fact, observe proof set update). | T1 (laptop, ~2 s) | ✅ confirmed — proof trees built deterministically; counterfactual retraction propagates | All 7 query types return correct proof trees: `grandparent(alice, dave)` → `[parent(alice, bob), parent(bob, dave)]` via grandparent rule. `cousin(dave, frank)` → `[parent(bob, dave), sibling(bob, carol), parent(carol, frank)]`. `cousin(dave, eve)` → 0 proofs (correctly NO). Counterfactual: retract `parent(bob, dave)` → `grandparent(alice, dave)` proof set goes from 1 to 0 deterministically. | OPENHUMAN_TL_MEMO §3 auditability claim landed concretely. Every derived answer surfaces every distinct rule chain that supports it; retraction is a primitive operation that propagates through the proof set without re-derivation overhead at query time. The same TL substrate doubles as the audit log — provenance is "free" at derivation time (small constant-factor overhead, not a re-run). Combined with exp65 (rule chains) + exp66 (negation), the SLM+TL composition has a complete Datalog-with-stratified-negation surface plus full provenance — every architectural claim in OPENHUMAN_TL_MEMO §1-3 is now operational. Caveat: proof-tree size is O(branching^depth) so for deep rules over large entity sets, full enumeration becomes expensive. Lazy / top-K proof generation is the natural extension. |
| 66 | `exp66_datalog_negation.py` | Extend the exp65 `<tl_rule>` protocol to support negated body atoms (`!rel(X, Y)`). Implement as: positive part via einsum chain (exp65) → for each negated atom, multiply by (1 − neg_tensor) element-wise. Stratified discipline: negated relations must be primitives or non-recursive rules; head variables must match negated atom variables (no existentials in negation). Test with uncle_strict, sibling_not_parent, parent_not_spouse, nephew, great_uncle. | T1 (laptop, ~1 s) | ✅ confirmed — set-difference absorbs cleanly into TL monoid | **9/9 cases pass.** sibling_not_parent (bob, carol) → True (siblings not parent); (alice, bob) → False (alice is parent, not sibling). parent_not_spouse (alice, bob) → True; (alice, henry) → False (spouse, not parent). nephew (dave, carol) → True via sibling(bob, carol) ∧ parent(bob, dave) ∧ ¬sibling(dave, carol); (dave, eve) → False (siblings). | The TL substrate's monoid (sigmoid/threshold + element-wise) absorbs negation naturally as `result * (1 - neg_tensor)` — same shape as the einsum-and-threshold chain, no architectural change. Adding a new filter rule that uses negation costs zero training. Stratification keeps semantics deterministic (no recursion through negation). Combined with exp65 (joins) and exp67 (provenance), the substrate now covers Datalog-with-stratified-negation completely. The OPENHUMAN_TL_MEMO architecture's claim that "every fact in any output traces to a KB row" is operationally demonstrated end-to-end. |
| 65 | `exp65_rule_chain_joins.py` | Extend the exp60b tool-call protocol from single-relation `<tl_closure>` to multi-relation `<tl_rule head="..." body="...">` Datalog-class rules (uncle, cousin, grandparent, great-uncle), implemented as einsum chains over per-relation tensors. Test on a small extended-family graph with parent + sibling primitives. | T1 (laptop, ~2 s) | ✅ confirmed — multi-relation joins work via einsum with no architectural change | **9/9 cases pass.** Verified: grandparent (`parent ∘ parent`), uncle (`sibling ∘ parent`), cousin (`parent⁻¹ ∘ sibling ∘ parent`), great-uncle (`sibling ∘ parent ∘ parent`). Each rule body parsed via regex, body atoms mapped to einsum operands, shared variables become contracted indices. Adding a new rule = adding one einsum chain over existing tensors; zero training. | The OPENHUMAN_TL_MEMO §1 multi-hop relational claim is now **operational**, not theoretical. The exp60b tool harness extends to Datalog-class rules without architectural change — the same einsum chain that did transitive closure now does arbitrary binary-relation joins. Combined with exp60a/b/c, the SLM+TL composition has a complete tool-call surface (closure + rules) that any SFT'd LM can target in exp60d. Real-scale concern: dense binary tensors at openhuman scale (10k entities → 400 MB per relation) push toward exp63's sparse substrate; for now, fits in RAM at synthetic test scale. |
| 64 | `exp64_clean_parity.py` | exp59C's parity test failed to expose TL's expressivity barrier because Hanoi's natural count vector `[2^(N-1), ..., 2, 1]` has parity `[0, ..., 0, 1]` — trivially monotone-separable. Re-test with irregular counts: (A) random small counts in [1, K], (B) counts from random suboptimal Hanoi solutions, (C) adversarial alternating-parity sequences. Predict sigmoid fails (monotone in count); cosine activation succeeds at α=π (per exp50 closed-form analysis). | T1 (laptop, ~5 s) | ✅ confirmed — sigmoid caps at majority-class on adversarial; cosine reaches 100% via grid search | **(A) Random K ∈ {4,8,16}, N ∈ {4,6,8}**: sigmoid hits 0.625-1.000 (caps at majority-class for non-monotone parity vectors); cosine **1.000 every case**. **(B) Random suboptimal Hanoi solutions**: sigmoid 0.500-1.000 (e.g., counts [243, 82, 14, 13] parity [1, 0, 0, 1] → sigmoid 0.500); cosine **1.000 every case**. **(C) Adversarial [1..8] alternating parity**: sigmoid 0.625, 2-mixture 0.625, **cosine 1.000**. | exp48 + exp50 + exp64 together establish the parity barrier cleanly: TL's monotone sigmoid + iteration cannot represent parity at any non-monotone count distribution; cosine activation at α=π would solve it but **gradient descent from random init cannot find this basin** (per exp50, sin(πx) vanishes at integer x where targets live). Conclusion: TL's expressivity barrier on parity is the **operator** (sigmoid + real arithmetic), not parameter count. To get parity in TL, change the operator class entirely (cosine + warm-start at α=π, complex-valued state, GF(2) semiring) — additional parameters within the sigmoid+iteration class can't help. Closes the exp59C honest miss. |
| 60c | `exp60a_kinship_traces.py`, `exp60b_tl_tool_harness.py`, `exp60c_rule_baseline.py` | The TL-as-tool integration line broken into 3 LM-free increments: (a) generate (graph, query, gold-tool-call, gold-answer) JSONL traces over synthetic kinship graphs, (b) parse `<tl_closure relation=... query=... subject=... object=...>` tags and execute against TL closure substrate with provenance trace, (c) "fake LM" rule-based emitter to validate the harness end-to-end before plugging in a real LM. If (c) doesn't hit 100%, the harness is buggy and exp60d (SFT) shouldn't run. | T1 (laptop, ~10 s for full eval) | ✅ all three pieces wire correctly; substrate proven before LM SFT | exp60a: 1000 train + 200 eval traces, hops distribution 0..5 (32/51/50/35/24/8 in eval). exp60b: 5/5 hand-written tool calls pass with full provenance chains (`alice → bob → carol → dave → eve`). exp60c: **200/200 (100.0%) eval accuracy** for the rule-based fake-LM at every hop count from 0 through 5. Tool calls are syntactically correct 200/200 times. | The substrate + harness + protocol layer of TL-as-tool is now provably correct — every variable in exp60d (real SFT) is pinned EXCEPT the LM's learning of the tool-call protocol. This is the cleanest possible test of "can a small instruct LM learn to invoke TL?" because any failure isolates to the LM, not the plumbing. Next: exp60d when LM access available — SFT a small instruct model (Qwen 2.5 7B / Llama 3.2 3B / Phi-3 mini) on the train.jsonl traces with LoRA, eval against the same eval.jsonl, compare to no-tool baseline. Falsifies if SFT'd LM accuracy at hops≥3 isn't ≥1.5× the no-tool baseline. NVIDIA Dynamo's KV-aware routing + cache-control hints are the natural serving layer for the (graph, query) prefix → tool-call → answer pattern; pin the prefix in cache, only re-decode after the tool result. |
| 59 | `exp59_harder_hanoi.py` | exp55 only tested TL on the easy slice (given recursive schema, Boolean monoid, matched initial state). Drop each assumption to find where TL substrate actually breaks. (A) Random initial state via state-space closure should hit a memory wall around N=11-12 (3^N states). (B) Cost-weighted shortest path requires min-plus semiring; TL's sum-then-sigmoid monoid should fail (per exp45 prediction). (C) Parity-of-moves-per-disk should fail (per exp48/50 — sigmoid monotone in count cannot express mod-2). | T1 (laptop, ~10 min for N up to 9 in part A) | (A) ✅ memory wall confirmed at N=10. (B) ✅ failure confirmed dramatically. (C) ⚠️ test ill-posed — predicted failure didn't manifest because Hanoi's natural count vector is monotone-separable. | (A) **OOM at N=10** (13 GB closure) and **120 GB at N=11**. Successful runs: N=9 (19,683 states) closure in 288 s, N=7 in 0.65 s, N=5 in <1 ms. (B) TL soft-min-plus surrogate (`exp(-β·cost)` then real-valued matrix powers, recover via `−log(R)/β`): **N=3 gap = 0.36** (4.6% undershoot of true min cost 7.76); **N=5 returns `inf`** (numerical underflow — multiplying many `exp(-β·c)` factors over long paths underflows float32). Genuinely fails to recover the min-plus answer at any reasonable N. (C) For all N=3,5,8,10, sigmoid(α·count + β) with the right (α, β) recovers parity exactly — but only because Hanoi's count vector is `[2^(N-1), 2^(N-2), ..., 4, 2, 1]` and parity vector is `[0,0,...,0,1]` (only the last disk has odd count = 1), which IS monotone-separable in count. Not a real parity test. | (A) **Real limit: ~10⁵ states for dense closure tensor.** Beyond that, sparse closure or hierarchical decomposition is needed. (B) **Real limit: TL's monoid is sum-then-threshold, not min-plus.** The exp45 falsification was about thresholded counts; this confirms the same barrier blocks shortest-path entirely. To get min-plus in TL form, need a different semiring (log-domain min via `−(1/β)·log Σ exp(−β·c)` with β→∞), or accept the geometric-mean-like surrogate as a different (possibly useful) quantity. (C) **Test was ill-posed; expressivity barrier from exp48/50 stands.** A clean re-test would generate parity targets where counts vary irregularly — e.g. parity along a randomly-perturbed Hanoi solution where disks move different non-power-of-2 numbers of times. Defer to exp60+ if this matters. Cumulative: TL substrate is correct up to memory-wall + monoid-mismatch limits. The novel-research question is now "how to add TL into a learned model," not "where TL alone breaks." |
| 58 | `exp58_hanoi_drift.py` | If Apple's "Illusion of Thinking" reasoning collapse is reproducible by simple per-step compounding execution error (i.e. the LRM's failure is well-modeled as "with prob ε, replace schema-correct move with random legal move"), then reaches-goal rate should fall like `(1−ε)^M` where M = 2^N − 1, predicting collapse near N≈10 at ε=0.01 (matches Apple's qualitative collapse window). Sweep N ∈ {5, 8, 10, 12, 15} × ε ∈ {0, 0.001, 0.005, 0.01, 0.05, 0.1, 0.3} with 30 seeds per cell. | T1 (laptop, ~30 s for full sweep) | ✅ confirmed — drift compounds; collapse window matches Apple | **ε=0.001 collapse window**: N=5 → 97% reach, N=8 → 83%, N=10 → 57%, N=12 → 0%, N=15 → 0%. **ε=0.01**: N=5 → 87%, N=8 → 17%, N=10 → 0%. **ε=0.1**: only N=5 retains any signal (10%). Empirical reach rates are CONSISTENTLY HIGHER than the (1−ε)^M lower bound — random legal moves don't always destroy the goal — but the qualitative collapse curve matches: any ε > 0 collapses at large enough N. ε=0 (TL substrate) holds 100% at all sizes through N=15 (and N=20 per exp55). | "Reasoning collapse on long-horizon execution = compounding error" is a defensible quantitative model. The substrate-side claim has a curve to defend now. The empirical-vs-theoretical gap is informative: reach rate decays slower than `(1−ε)^M` because some random moves are recoverable (the Hanoi state graph has redundant paths to goal); but at large N, even tiny ε collapses the system because the schema becomes incoherent after divergence. **TL is at the ε=0 column by construction; LRMs sit at ε ∈ [0.001, 0.05].** Next: (a) re-run with two noise models — "random legal move" vs "schema-coherent recovery" (try to re-plan after drift) — to separate "drift kills you" from "lack of recovery kills you"; (b) compare empirical decay to fitted `(1−ε_eff)^M` for various ε to back out an effective drift rate per LRM from Apple's curves. |
| 57 | (deferred — needs LM access) | — | — | (deferred) | — | dropped from this batch per user direction; novel direction is now "teach a model to USE TL," see exp60+ ideas in `IDEAS.md`. |
| 56 | `exp56_river_crossing.py` | Apple's River Crossing collapses LRMs at N=3 (11 moves). Frame the puzzle as TL transitive-closure reachability over the legal-state graph. Should solve classic N=3 instantly and scale until 2^(N+1) state-space exceeds memory. | T1 (laptop, ~1 s for full sweep through N=10) | ✅ confirmed — TL solves classic N=3 trivially; scales to N=10 (2048 states) in ms | **Classic farmer/wolf/goat/cabbage**: 10 legal states, 3 closure iterations, reachable=True, <1 ms. **Generalized N-item with random pairwise conflicts**: N=10 → 1024 legal states, 5 iters, 35 ms closure. Reachability often False at N≥6 because random conflict graphs make goal genuinely unreachable — but the closure substrate doesn't break. State-space size 2^(N+1) is the real limit (would hit memory wall at N≈15-16). | Second Apple-puzzle solved by TL substrate; broadens the substrate-of-execution argument from deterministic enumeration (Hanoi) to constraint-graph search (River Crossing). Both Apple's reported collapse points are inside TL's trivial regime. Real limitation is the dense closure tensor at large state spaces; same wall as exp59 (A). For larger N, would need sparse closure or symbolic state-space compression. |
| 55 | `exp55_tl_hanoi.py` | A TL-style substrate (state tensor `S[disk, peg]`, min-reduction legality, deterministic tensor update for moves) executes the recursive Tower of Hanoi algorithm reliably to N=20 (2^20 − 1 ≈ 10^6 moves) where Apple's "Illusion of Thinking" reports frontier LRMs (o3-mini, DeepSeek-R1, Claude-thinking) collapse to ~0% accuracy beyond N≈8-10. Zero learned parameters; substrate is constitutionally deterministic. The point: separate "knows the algorithm" from "has a substrate to execute it without drift". | T1 (laptop, ~50s for full N=3..20 sweep) | ✅ confirmed — perfect execution at every size | **N=3 → 20: 100% legal moves, 100% reached goal at every size.** N=20: 1,048,575 moves in 36.9s on CPU; N=15: 32,767 moves in 1.1s; N=10: 1,023 moves in 0.03s. All N=8..20 sit inside or beyond Apple's reported LRM-collapse window. Number of moves = 2^N − 1 exactly (= optimal) at every size. | "LRM reasoning collapse on Hanoi at large N" is a **substrate-of-execution problem, not a reasoning problem**: the algorithm is 6 lines, the difficulty is maintaining state across exponentially many steps in a token stream. TL's typed-state recurrence is the right substrate where the rule is known and the work is deterministic long-horizon execution. This complements exp44/47/53 (TL substrate for monotone closure on real graphs) — both rest on the same claim: when the operator basis matches the problem, a tiny TL formulation crushes both pure-LM and pure-MLP scaling. Caveats: (a) doesn't test *learning* the rule from demonstrations — the rule is given. Learning the rule is exp44 territory. (b) Lawsen's rebuttal (arXiv 2506.09250) argues much of Apple's collapse is output-token truncation; this experiment doesn't refute that, it just demonstrates the substrate-side fix. (c) The interesting research target is **LM-as-rule-extractor + TL-as-executor** (exp57 in IDEAS.md): if LRMs can write the function but not enumerate, hand the function to a TL substrate. Next: exp56 (River Crossing as constraint-graph reachability — Apple's other puzzle), exp57 (LM→TL hybrid Hanoi where the LM emits the recursion and TL executes), and a noise-robustness sweep simulating LRM drift (probability ε of choosing a random legal move per step, sweep N and ε to show how compounding error reproduces Apple's collapse curve). |
| 50 | `exp50_cos_parity.py` | exp48 showed sigmoid+iteration can't express parity at any (α,β,γ,δ). Hypothesis: changing the **operator** to a periodic activation `act(x) = (1−cos(πx))/2` (period-2, GF(2)-compatible at integer x) breaks the ceiling. Frozen ideal weights (α=1, β=0, γ=0) should give F1≈1 because `act(R@A)` directly computes path-count mod 2 at integer x. Tested 4 configs vs exp45 parity target P = (I+A+A²+...) mod 2: frozen-ideal, train-from-ideal, train-from-closure-init, train-from-random. | T1 (laptop, ~3 min) | ❌ falsified — operator change is not sufficient; the recurrence structure itself is the barrier | **frozen_ideal F1=0.078 ± 0.010** (gradient vanishes — sin(πx)=0 at integer x where targets live), **train_ideal F1=0.078** (stays put), **train_closure F1=0.353 ± 0.044**, **train_rand F1=0.439 ± 0.116**. None beat sigmoid+cross-term TL_OR4 (F1=0.69). | The "operator is the barrier" hypothesis from exp48 is **partially wrong** — operator matters, but it's not sufficient. The deeper issue: **R₀=A and R_{k+1}=act(R@A, R) overwrites the state**, so once one mod-2 step happens the path-count accumulator is destroyed. Path-count parity P = (I + A + A² + ... + A^n) mod 2 needs an explicit accumulator: `C ← I + A·C mod 2`. The TL recurrence form `R ← f(R@A, R)` is a **fixpoint operator over monotone updates** — parity isn't a fixpoint of any monotone op; iterating-and-overwriting cannot accumulate it. Combined with exp48: two candidate fixes to TL's parity collapse (more params; periodic operator) **both fail**, for different reasons. The actual fix needs a structurally different recurrence — residual accumulator, complex-valued state, or direct GF(2) matrix algebra — not parameter or activation surgery within the existing TL form. Next: (a) test residual-accumulator TL `C ← σ(C + A·C)` or `C ← (C + A·C) mod 2` straight-through, (b) characterize formally what fixpoint class TL's recurrence can express (likely related to monotone-circuit / least-fixpoint logic), (c) accept this as a hard structural boundary and stop trying to fix parity — pivot to confirming TL_OR3 utility on richer monotone code tasks (null-reachability, taint). |
| 49 | `exp49_crossterm_imports.py` | Does the Hadamard cross-term help on real-code transitive closure (exp44 import-closure setup)? Hypothesis: code-closure is monotone/OR-like, so γ_X stays near 0 and TL_OR4 ≈ TL_OR3. If γ_X moves significantly, code has non-monotone substructure worth investigating. Sweep n_train ∈ {20, 100, 500} × 3 seeds × 4 eval conditions (in_dist, real Python imports, OOD sparse, OOD dense). | T1 (laptop, ~3 min) | ✅ confirmed — cross-term inert on monotone code-closure | **TL_OR3 ≡ TL_OR4** across all 12 cells. F1 identical to 3 decimals (in_dist 0.997-0.999, real **1.000**, ood_sparse 1.000, ood_dense 0.998-0.999). **γ_X stays small and positive**: +0.21±0.08 (n_train=20), +0.21±0.01 (100), +0.24±0.00 (500) — opposite sign from parity (γ_X=−3.6 in exp48). Trained α≈4.3, β≈6.9, δ≈−3.99 nearly identical to TL_OR3's α/β/γ at the closure operating point. | The cross-term is **non-monotone-only capacity**: γ_X→0 on monotone tasks, γ_X→large-negative only when the task has XOR-like substructure (which TL still can't actually express, per exp48). Cost-benefit is clean: free to add (no harm on monotone tasks), no help unless the task is non-monotone — and even then, sigmoid+iteration caps the benefit at "attenuated OR" (exp48). Code transitive closure is firmly OR-like; standard 3-param TL is the right baseline for code analysis. Combined with exp44 (TL beats MLP on real imports) and exp47 (zero-shot scaling to n=128), the "TL_OR3 is the right primitive for monotone code-graph tasks" case is strong. Next: (a) test on null-reachability or taint analysis to confirm on a richer code task; (b) exp50 cosine-activation TL on parity — test whether changing the *operator* (not parameter count) breaks the exp48 ceiling. |
| 48 | `exp48_crossterm_xor.py`, `exp48_verify_xor.py` | A 4-param TL with multiplicative cross-term `R ← σ(α·R@A + β·R + γ_X·(R@A)⊙R + δ)` should crack the exp45 parity collapse by adding a 4th independent pre-sigmoid coefficient — enough to place each binary input pair at its own value (XOR shape: -, +, +, -). **Verification protocol** (added after the original exp48 commit mis-celebrated this): freeze handcrafted XOR-init weights (α=4, β=4, γ_X=-8, δ=-2) and evaluate without training — if the architecture truly expresses XOR, F1 should be near 1.0. | T1 (laptop, ~5 min total) | ⚠️ **partial — F1 measurement real, architectural-novelty claim falsified** | **Trained TL_OR4** parity F1=0.685 vs TL_OR 0.256 vs MLP 0.635 (genuine improvement, reproducible). **But frozen XOR-init weights** score parity **F1=0.382 ± 0.006** — the alleged "XOR operating point" cannot compute parity. SGD trained-from-XOR-init **drifts AWAY** from XOR-init: γ_X swings -8 → -4.1, β swings 4 → 6.4, α swings 4 → 2.3 — moves toward a closure-attenuated basin, not toward XOR. Random init (α, β, γ_X, δ ∼ U[-2, 2]): parity F1=0.185 ± 0.076 (high variance, mostly fails). Closure-shaped init was privileged. | **The architecture cannot express parity at all.** F1=0.685 isn't SGD finding XOR — it's the ceiling of "smart attenuated-OR" (closure with (1,1) case partially suppressed via γ_X). The real barrier is **sigmoid + iteration in real arithmetic**: σ(±2) = (0.12, 0.88) are not Boolean, and K=4 iterations smear values further. Sigmoid is not a homomorphism over GF(2) — no choice of (α, β, γ_X, δ) makes real-valued sum-of-paths compose to mod-2 path-count. The 4-param extension gives one more knob within TL's existing expressive class but does NOT expand the class. **Original exp48 framing ("cross-term cracks parity") was wrong**, caught by the frozen-init verification. To actually crack parity, TL needs a non-sigmoid nonlinearity (cosine, complex-valued, modular activation) or GF(2) semiring (binarized + mod-2 accumulator). Don't scale this on parity — scaling an attenuated-OR ceiling is pointless. The honest mechanistic finding is more useful than the wrong celebratory one: the expressivity barrier is the operator class (sigmoid, real arithmetic), not parameter count. Next: (a) test cosine-activation TL on parity — `R ← cos(α·R@A + β·R + γ)` does have GF(2)-like accumulation properties; (b) GF(2) semiring TL with binarized R and XOR composition; (c) characterize formally which Boolean operations are computable by `sigmoid({1, R, R@A, R⊙(R@A)}-linear after K iterations)` — connect to threshold-circuit complexity. |
| 47 | `exp47_size_generalization.py` | TL's exp44 closure win scales to bigger graphs **for free**: train the 3-param TL once at n=16, evaluate zero-shot at n ∈ {32, 64, 128} with K=ceil(log₂(n)) iterations. Sparsity p=2/n keeps avg degree ~2 (Python-import-like). Hypothesis: TL F1 stays ≥0.95 across all sizes (params are size-independent); MLP — re-trained from scratch at each n — degrades as n grows because flat n²-dim I/O learning gets harder. | T1 (laptop, ~5 min for 12-run sweep) | ✅ confirmed — strongest scaling result in the project | **TL trained ONCE at n=16 (3 params: α=4.34, β=6.87, γ=-3.96) → F1 zero-shot**: n=16 **0.999**, n=32 **0.998**, n=64 **0.998**, n=128 **1.000**. **MLP re-trained at each size from scratch with 500 graphs**: n=16 0.529 (37k params), n=32 0.204 (136k), n=64 0.009 (532k), n=128 **0.000** (2.1M params). Closure density drops with n (0.090 → 0.016 at p=2/n) — MLP can't escape predict-all-zeros local minimum at high n. | The "TL is the right primitive" claim now has real legs. **3 parameters trained on toy 16-node DAGs hit F1=1.000 on 128-node graphs zero-shot**, while a 2.1M-parameter MLP re-trained directly on 128-node graphs collapses to F1=0. The TL recurrence encodes the closure RULE in 3 size-independent scalars; MLP encodes a specific n²-dim mapping that breaks under size shift and gets buried by sparse-output BCE collapse at high n. Combined with exp44's real-import-graph zero-shot win, this is the strongest TL-for-code evidence: **the rule, not the data, is what TL captures, and the rule generalizes for free**. Real codebases have 100s-1000s of modules — TL handles them with 3 params trained on tiny synthetic graphs. Caveats: (a) MLP at h=64 may be under-parameterized at n=128 — could try h=512 or transformer to confirm capacity isn't the issue; (b) closure density crashes at p=2/n large n, making the BCE optimization landscape adversarial for MLP — could fix that with focal loss or class weighting. But the qualitative gap (1.000 vs 0.000) is too large to be just a tuning issue. Next: (a) validate at n=512, n=1024 (real-codebase scale); (b) confirm MLP's failure isn't fixable by capacity/loss-tuning; (c) test whether TL's scale-free property holds for multipath2 (where TL still wins, exp45) — if yes, the size-independence is general to the TL monoid, not just closure. |
| 46 | `exp46_shortest_path.py` | TL's recurrence `R ← σ(α·R@A + β·R + γ)` is **monotone in A** — adding edges can only increase outputs. Shortest-path distance is **non-monotone**: adding the direct edge (i,j) flips `1[SD(i,j)==2]` from 1→0. Test 4 binary tasks at n=16, p=0.30: `sd_eq_1` (predict A, trivial), `sd_eq_2` (2-hop ∧ ¬1-hop, non-monotone), `sd_geq_2` (closure ∧ ¬1-hop, non-monotone, ~13% density), `sd_geq_3` (closure ∧ ¬1-hop ∧ ¬2-hop, non-monotone, sparse ~3% density). Hypothesis: TL_OR fails non-monotone tasks; MLP wins. | T1 (laptop, ~3 min for 36-run sweep) | ⚠️ partial — non-monotonicity alone is not sufficient to break TL; non-monotonicity + sparsity does | **sd_eq_1 F1**: TL_OR=0.13, TL_LIN=**1.00** (β=2.05 → linear passthrough), MLP=0.57. **sd_eq_2 F1**: TL_OR=0.43, TL_LIN=0.28, MLP=**0.47**. **sd_geq_2 F1**: TL_OR=**0.60**, TL_LIN=0.34, MLP=0.59 — *TL_OR ties MLP on a non-monotone task at moderate density*. **sd_geq_3 F1**: TL_OR=**0.007**, TL_LIN=0.000, MLP=**0.177** — TL collapses, MLP also bad but non-zero. | Three findings, none clean. (1) Non-monotonicity alone doesn't kill TL: at moderate density (sd_geq_2), TL_OR is competitive with MLP because the closure-shaped recurrence still captures most of "is reachable". TL learns to deal with the "AND NOT direct" suppression weakly. (2) **Non-monotonicity + sparsity is the real falsification**: sd_geq_3 (2.8% density, requires closure ∧ ¬1-hop ∧ ¬2-hop) collapses TL_OR to F1=0.007, MLP to F1=0.18 — both bad, but TL is uniquely zero. (3) **Side discovery**: TL_OR's forced iteration HURTS on trivial linear tasks like sd_eq_1 (just predict A) — TL_LIN's flexibility (β can drive linear passthrough) wins F1=1.00 while TL_OR is trapped at F1=0.13. The iterative structural prior is a *commitment* — useful when the task fits, harmful when it doesn't. Caveat: even MLP is poor on sd_geq_3 (F1=0.18) so this isn't a clean TL-vs-MLP discriminator at this scale; need bigger graphs or higher-density sparse tasks for a sharper test. Combined with exp45 (parity collapse), the falsification picture: TL fails on (a) XOR/parity (different semiring) and (b) sparse non-monotone targets (structurally inexpressible). Next: (a) try min-plus shortest path REGRESSION (TL's sigmoid clips, reveals expressivity limit), (b) test a non-graph compositional task — e.g., n-step Boolean circuit evaluation where TL should win, validating the converse claim. |
| 45 | `exp45_pathcount_falsify.py` | TL's win in exp44 is **structure-matching**, not "TL universally beats MLP". Three tasks on the same n=16, p=0.30 DAGs: (1) closure (TL's home turf), (2) multipath2 = "≥2 distinct paths exist" (additive aggregation, expected to break TL), (3) parity = "(# distinct paths) mod 2" (XOR semiring, definitely outside TL's monoid). Three models: TL_OR (sigmoid), TL_LIN (no sigmoid), MLP (~37k params). | T1 (laptop, ~3 min for 27-run sweep) | ✅ confirmed (with nuance — TL is broader than expected) | **Closure** TL_OR=0.998, TL_LIN=0.999, MLP=0.810. **Multipath2** TL_OR=**0.934**, TL_LIN=0.746, MLP=0.714 — *TL still wins*: optimizer tunes α=3.6, γ=-3.8 so sigmoid saturates only when count≥2 (single 2-hop path → pre-sigmoid≈-0.2 → output 0.45 → labeled negative). TL's effective monoid is **thresholded sum-over-paths**, broader than pure Boolean OR. **Parity** TL_OR=**0.256**, TL_LIN=0.516, MLP=**0.635** — *clean TL collapse*. TL_OR's F1=0.256 is below the predict-all-positive baseline (~0.40 at 25% density). TL_LIN's recovery to 0.516 (vs TL_OR 0.256) confirms sigmoid is the specific killer for XOR-shaped tasks. | Falsification confirmed: when the task semiring is fundamentally non-additive (parity = XOR = GF(2) matrix arithmetic), TL not only loses but goes below random — sigmoid-thresholded sum cannot represent XOR. **Refines exp44**: TL's inductive bias is "thresholded sum-over-paths", a broader class than "Boolean OR" — it can express "≥k paths" by tuning (α, γ) thresholds, not just "≥1 path". Pure Boolean OR is a *special case* of the TL monoid, not the full extent. The clean structure-matching story still holds: TL wins task semirings within {OR, ≥k-count, sum-then-threshold}; loses on {XOR, parity, max, min} that require different aggregation primitives. Combined with exp44, the project's TL-for-coding thesis is: TL is the right primitive for problems with multiplicative composition + additive aggregation + monotone thresholding (closure, reachability, count-thresholds, transitive properties). It is the wrong primitive for problems requiring max/min (shortest path, best-route), XOR/parity (modular arithmetic, error-correcting codes), or non-tensor-product structure. Next: (a) test min-plus semiring (shortest-path distance) — TL should also fail; (b) test on a non-graph compositional task — n-step Boolean circuit evaluation, TL's home turf; (c) write up exp44+45 as a coherent piece since they form one falsifiable claim about TL's expressivity. |
| 44 | `exp44_import_closure.py` | TL's einsum recurrence (`R ← σ(α·R@A + β·R + γ)`) parameterized with **3 scalars** matches transitive-closure structure exactly; on Python-import-style sparse DAGs, the 3-param TL should beat a high-capacity MLP (~40k params) on in-distribution F1 AND generalize zero-shot to (a) the real tensor-project import graph and (b) OOD sparsity (p=0.05, 0.25), while MLP overfits the training distribution. The structural prior pays off most at low data. | T1 (laptop, ~3 min for 18-run sweep + ablation) | ✅ confirmed (with init caveat) | **TL (3 params, analytic init α=5, β=5, γ=-2.5): F1=0.997-1.000 across all 4 conditions at every data size** (n_train ∈ {20, 100, 500}, 3 seeds). Real tensor-project import graph zero-shot: **F1=1.000 even at n_train=20**. **MLP (37,248 params, ~12,400× TL): F1 = 0.19/0.35/0.51 in-dist, 0.14/0.31/0.38 real, 0.18/0.33/0.36 ood-sparse, 0.14/0.33/0.61 ood-dense across n_train=20/100/500.** Trained TL params drift to α≈4.4, β≈6.9, γ≈-4.0 (refined sharper). **Ablation — neutral init (α=1, β=1, γ=-1)**: TL converges to α≈4.2, β≈2.5, γ≈-2.8, F1=0.36-0.59 in-dist, 0.58 real — still beats MLP at matched data, but no longer crushing. | The structural prior story works **when combined with a sensible init**: closure-shaped recurrence + 3 scalars at the analytic operating point gives perfect zero-shot transfer to real Python imports, OOD sparsity, OOD density. 12,000× more parameters in an MLP can't even half-match this on held-out graphs. Cleanest TL-vs-MLP positive in the project so far. **Caveat**: with neutral init, BCE on sparse targets (~10% positive cells) pulls γ too negative early and the optimizer settles at a local minimum where β is too small to preserve direct edges (F1=0.58 vs 1.00 at analytic init). So the result is "structure is necessary but not sufficient — needs a good operating-point init OR weighted BCE OR reparameterization that scales γ with sparsity". The "init" isn't arbitrary — it's the analytic Boolean-OR-of-matrix-powers approximation — but it shows pure SGD on a non-convex 3-d landscape can fail. Next: (a) reparameterize TL as `R ← σ(c · (R + R@A − τ))` with single learnable temperature, see if optimizer finds the operating point; (b) scale to n=64/256 nodes with K=log₂(n) iterations; (c) test on tasks where closure ISN'T the right structure (counts, additive aggregations) — TL should LOSE there, validating that the win is structure-matching, not "TL is universally better". |
| 43 | `train_phase7_planning.py` | TL's sample-efficient forward model translates into better downstream planning success (sampling-MPC navigation to corners on no-collision world) — the inductive-bias advantage on point predictions should compound across tasks. | T1 (laptop, ~40s) | ❌ falsified | TL aggregated success rate: 10.6% / 9.7% / 9.4% across n ∈ {50, 200, 2000}. MLP h=64: 55.6% / 53.1% / 60.0%. MLP h=128: 52.8% / 68.6% / 72.8%. TL's avg_steps_to_success is 1.3 (only succeeds when start is adjacent to goal — doesn't actually plan navigation). MLP's avg_steps_to_success is 7.2-7.7 (real planning happening). | TL's sharp predictions don't compose well under multi-step fuzzy rollouts, AND don't give cumulative-goal-prob MPC any hill-climbing signal (most random sequences score 0 → argmax over zeros is random). MLP's smoother outputs leak mass broadly → planner can hill-climb. Partly a planner-fairness issue (sampling-MPC favors fuzzy predictors), partly a real TL limitation. Either way the practical conclusion is: TL's sample-efficiency on point predictions does NOT translate into planning advantage at this scale. To test TL fairly with planning, would need either (a) a planner robust to sharp predictions (BFS over discrete predicted next-states, or distance-shaped reward), (b) test on a task that doesn't require multi-step planning (single-step decision). |
| 42 | `train_phase6a_sample_eff.py` | TL's structural prior gives **better sample efficiency** than MLP — at low data sizes, TL should win because the prior compensates for missing data; MLP should catch up at scale. | T1 (laptop, ~4 min for 48-run sweep) | ⚠️ partial — TL wins in-occluder recall at low data; loses overall P@2 at every data size. Bias-vs-capacity tradeoff visible cleanly. | Across n ∈ {50, 200, 500, 2000} trajs, fixed 1000 grad steps. **Overall P@2**: TL flat at 88.0% across all sizes (structural ceiling); MLP h=128 scales 90.5% → 92.9% → 92.7% → 92.4%. **In-occluder recall**: TL=95.5/97.5/98.2/97.9 vs MLP h=64=91.1/93.3/97.5/99.9. **Crossover at n≈500 for in-occluder recall.** TL also has 2-3× lower variance at small n (TL ±1.9 vs MLP ±5.0 at n=50). | Cleanest positive TL signal yet: TL has a sample-efficient advantage on the structural sub-task (hidden-object recall) at small data, with crossover around n=500. Above that, MLP capacity wins. The textbook bias-vs-capacity tradeoff plays out exactly as predicted by inductive-bias theory. TL never beats MLP on overall P@2 (its absolute ceiling holds). Honest scope: "TL prior gives 4pp sample-efficient advantage for object-permanence-style sub-tasks at n<500; capacity wins at scale on overall accuracy." Next: this validates the prior-pays-off-when-data-is-scarce story, motivating a multi-task transfer test (phase 7) where the dynamics model is shared and reused — that's the regime where sample efficiency compounds. |
| 41 | `train_phase5_transfer.py` | TL's per-cell einsum prior gives **better OOD generalization** (smaller accuracy drop under distribution shift) than an MLP at matched/larger params. Train on N=2 + center occluder + no-collision; test on object-count (N=1/3/4) and occluder-position (TL, BR) transfer. | T1 (laptop, ~2 min for 12 trainings × 6 conditions) | ⚠️ partial — TL has flatter transfer curves but MLP still wins absolute accuracy in every condition. | **Absolute P@N**: MLP wins everywhere (in-dist: TL 87.8% vs MLP h=128 92.3%). **Transfer drop (P@N)**: TL drops less than every MLP except on N=1 (TL -3.1pp vs MLP h=64 -3.7pp; TL N=4 -5.0pp vs MLP h=256 -7.9pp; TL occ-shift -5.0pp vs MLP h=256 -10.5pp). **In-occluder under count transfer**: TL holds 99.8-99.9% at N=3/4 vs MLP h=128/256 dropping to 86-92% (MLP h=64 also holds at 99.8%). **Occluder-position transfer**: ALL models collapse on in-occluder recall (TL 20%, MLP <10%) — neither has translation invariance. | Weak partial confirmation: TL trades absolute accuracy for flatter transfer curves; the inductive-bias-for-generalization story survives in modest form. The clearest TL-specific win is **N→larger transfer for hidden-object recall**: TL's per-cell processing is N-invariant by construction. Largest models (MLP h=128/256) overfit to the 2-object input distribution and degrade at N=3/4. Occluder-position transfer is a setup ceiling — all models trained with center-only occluder and the recurrent belief learned to invoke the prior only at center positions. Next options: (a) **translation-equivariant TL** parameterized by relative shifts `W[a, dx, dy]` would test whether spatial invariance fixes occluder transfer; (b) abandon gridworld and test TL on compositional rule-learning tasks (Domingos's actual claim, finite-domain logic) which is TL's true sweet spot. |
| 40 | `mlp_model.py`, `train_phase3c.py`, `train_phase3d_sweep.py` | TL+memory beats an equal-param MLP+memory baseline on the phase 3b world (collision + occluder + single-channel state); the einsum factorization is the active ingredient, not just the recurrence. | T1 (laptop, ~1 min for 12-run sweep) | ❌ falsified | 3-seed sweep: TL+memory P@2=**91.20% ± 0.61**, in-occ=98.64% ± 0.67. **MLP h=64 (8,576 params, HALF of TL) wins both: P@2=95.69% ± 0.55, in-occ=99.76% ± 0.42.** MLP h=128/256 also beat TL on P@2. (Earlier single-seed phase 3c writeup mistakenly cited 99.16% for TL — was a misread of phase 3b's in-occ-recall=99.x%, actual P@2 was 90.63%.) | Memory is the active ingredient (any model+memory > no-memory by 30 pts on in-occluder recall), not TL. TL's per-cell W tensor can't represent collision (a cross-cell AND condition) → structural ceiling at ~91%. Result corroborates Nov 2025 OCWM literature (arXiv 2511.06136: object-centric world models also break under multi-object interactions). Next: phase 5 — does TL's prior give OOD generalization (object-count / occluder-position transfer) that MLP doesn't, on a no-collision world where TL's assumptions hold? |
| 39 | `train_phase3.py`, `train_phase3b.py` | An SSM-style belief tensor (recurrent: `belief = obs + (1−obs)·forward(belief, action)`) on top of a TL einsum forward model recovers object permanence under partial observation (occlusion + identity loss) — the no-memory baseline cannot. | T1 (laptop, ~3 min) | ✅ (recurrence works), but ❌ on TL-specific framing — see exp40 | Phase 3a (no memory): P@2=87.27%, in-occluder recall=67.55%. Phase 3b (+ belief tensor): P@2=90.63%, **in-occluder recall=98.84%**, stable across 20-step trajectories (88-92% throughout). | Adding memory to a 16K-param TL forward model jumps hidden-object recall by 31 pts and stays stable over time — recurrence is doing real work. Caveat: did NOT compare to MLP-with-memory at equal params, so gain may not be TL-specific. Next: phase 3c with MLP baseline; phase 3d with explicit Spelke continuity prior as a TL constraint. |
| 38 | `world.py`, `train.py`, `train_phase2.py` | A TL einsum forward model (`R[t+1] = sigmoid(einsum(R[t], W[action]))`) can fit deterministic 2D gridworld dynamics from random rollouts. | T1 (laptop, <1 min) | ✅ trivial | Phase 1 (deterministic, 8×8, 2 objects): val_acc=100%. Phase 2 (+ collision rule): val_acc=99.1%, 10-step rollout 99.9→97.3%. | Confirms einsum + sigmoid + Adam pipeline works, but trivially — model has exactly 16K params for a 16K-cell action-conditioned lookup table. Plumbing validation, not science. The real test of TL is phase 3 (single-channel + occlusion → exp39). |
| 37 | `exp37_tl_inductive_bias.py` | A TL auxiliary loss (MSE between a relation-head readout and the transitive-closure target) makes a from-scratch transformer generalize better to deeper hops. | T1 (laptop, ~30 min × 6 runs, MPS) | ❌ null | shallow Δ=-0.008 (V=0.614, T=0.606); deep Δ=+0.009 (V=0.328, T=0.337); 3 seeds, both within seed σ≈0.07–0.11. TL loss did converge 0.26→0.13. | Aux loss on a parallel relation head doesn't transfer to the LM head — gradients flow back through the encoder, but the LM head never has to consume closure features. Also: floor too low (33% deep, near chance) so we couldn't detect a real signal anyway. Next: lift the floor (more params/data) and wire closure into the residual stream as architecture, not as a side loss. |
| 36 | `exp36_code_dependency.py` | Pretrained GPT-2 (base, 124M) can answer multi-hop code-dependency reachability questions zero-shot via in-context call-graph facts; a TL-precomputed transitive-closure tool injected as context further boosts accuracy. | T1 (laptop, ~30s on MPS) | 🛑 invalid — model too weak to discriminate conditions | All three conditions tied at exactly 0.500 across depths 1/2/3 (depth-4 had 0 positives in DAG). Sanity check on raw logits: GPT-2 has constant +0.2 to +0.8 logit bias toward " No" regardless of prompt content, **even when context literally says "f00 transitively calls: f01, f05" and we ask "does f00 call f05?"** (yes=-3.31, no=-2.52, Δ=-0.79). Model just emits its prior — can't even copy from immediately preceding context. | GPT-2 base (124M, not instruction-tuned) is below the instruction-following floor needed to score this task — the experiment doesn't discriminate alone vs facts vs tool-use. Not a TL story; a model-capability story. To test the tool-use hypothesis fairly, need an instruction-tuned model (Llama-3.1-8B-Instruct, Phi-3, or similar) or score by perplexity over the full answer. Skipping that rabbit hole — moving to exp44 (TL-as-substrate on Python import graphs) which targets TL's actual sweet spot (discrete, finite-domain, compositional structure) without relying on a frozen LM's instruction-following. |
| 35 | `exp35_two_tower.py` | Two-tower (Possibility, Actuality) tensors with T ≤ P enable counterfactual queries without polluting facts. | T1 | ✅ | T fits MSE=0.006, max constraint violation 0.0019, post-query Δ=0 | Mechanism works as designed; the architecture supports counterfactuals cleanly. (Mildly trivial since copying preserves source.) |
| 34 | `exp34_energy_based.py` | Energy-based reconstruction with tree+cycle constraints beats local sigmoid on a KG with conflicting facts. | T1 | ❌ | local F1=0.818 (FP=4, FN=0); energy F1=0.714 (FP=0, FN=4) | They make opposite errors. Constraint at λ=5.0 is too strong — kills true edges to satisfy tree property. Needs better λ tuning or soft constraints. |
| 33 | `exp33_tight_softor.py` | A tighter logit-space soft-OR operator (max-pool / low-T logsumexp / noisy-OR) makes log-odds tensor logic give F1≥0.95. | T1 | ⚠️ partial | logit-space ops all F1=0.485; **noisy-OR in PROB space: F1=1.000** | The fix is to STAY in probability space and use noisy-OR (1 − ∏(1−p)) — log-space OR operators all upper-bound too aggressively. |
| 32 | `exp32_attention_compose.py` | GPT-2 attention heads compose like tensor-logic rules: A_parent ∘ A_parent ≈ A_grandparent. | T1 (laptop, ~2 min) | ❌ falsified, but informative | clean-head Pearson=0.192 (threshold was 0.3); 2.45× baseline ratio | GPT-2 learns parent and grandparent as **separate** dedicated heads (L11H8, L0H6), not compositionally. Suggests transformers don't internally do TL-style composition. |
| 31 | `exp31_surprise_gated.py` | Surprise-gated updates beat uniform for both convergence and retention. | T1 | ⚠️ technically passes, practically weak | uniform: 260 ep / forget A→32; surprise: 300 ep / forget A→30 | Both models forgot phase A almost entirely. Surprise gating barely helps at this scale; needs bigger model or different formulation. |
| 30 | `exp30_init_fix.py` | Initializing absent-edge logits to -LARGE eliminates the sigmoid floor at all T. | T1 | ⚠️ direction confirmed, magnitude matters | init=-10 → F1=1.0 across T∈[0.1, 2.0]; init=-6 leaks at T=2.0 | Floor is essentially an init bug; init magnitude must scale with max T. Updates exp1's verdict. |
| 29 | `exp29_log_odds.py` | Pure log-odds fixpoint eliminates the sigmoid floor problem. | T1 | ❌ but illuminating | log-odds F1=0.485, log-odds+calib F1=0.552, **naive sigmoid with proper -LARGE init: F1=1.000** | The floor problem was an artifact of using logit=0 for "absent" edges, not of sigmoid itself. Fix: init absent-edge logits to a strong negative. Logsumexp soft-OR over-saturates. |
| 28 | `exp28_unified_system.py` | Semantic embeddings beat random ones for relational learning. | T1 | ✅ | random F1=0.077, semantic F1=1.000 | Need transformer-grounded `E_i` for any real KG → motivates exp31 (composed-attention) |
| 27 | `exp27_blocks_world.py` | Tensor-logic rules can plan via BFS over relation states. | T1 | ✅ | finds plans in toy blocks-world | Planning works structurally; representations too thin → exp32 (richer state) |
| 9 | `exp9_tannealing_fix.py` | T-annealing alone fixes the sigmoid floor problem. | T1 | ❌ | annealing keeps halluc=1.0; calibration (margin=0.3) gets F1=0.84 | Need baseline subtraction, not just T → exp29 (log-odds) |
| 8 | `exp8_ssm_tensor_logic.py` | SSM A-matrix as tensor-logic rule outperforms vanilla RNN. | T1 | ❌ | RNN=1.00 acc, TL-SSM=0.547 | Conceptual bridge real, perf win not demonstrated → revisit at scale (T2) |
| 7 | `exp7_violation_of_expectation.py` | Adding a physics rule helps detect physics violations. | T1 | ❌ | half-rule: 5.5× higher loss, *less* surprise on violations | Partial priors backfire → either full prior or none |
| 6 | `exp6_multirelation_kg.py` | Bilinear embeddings can compose relations zero-shot. | T1 | ❌ | symbolic Uncle: F1=1.0; bilinear: F1=0.059 (random) | Composition is free symbolically, not in embedding space → strongest argument for tensor logic |
| 5 | `exp5_rule_sparsification.py` | L1 needed to find which einsum rule is right. | T1 | ⚠️ | even λ=0 → wrong rules go to 0.001 | Gradient already does it; L1 unnecessary on toy scale |
| 4 | `exp4_curiosity_gridworld.py` | Prediction-error curiosity beats random exploration on coverage. | T1 | ❌ | curiosity Gini=0.71 (stuck at boundary), random Gini=0.21 | Noisy-TV problem real → exp33 (count-corrected curiosity already in exp11) |
| 3 | `exp3_generative_replay.py` | A small GMM "dreaming" old data beats EWC for continual learning. | T1 | ✅ | GMM=97.8%, EWC=70.7%, hybrid=99.0% | Dreaming compresses experience extremely well → use for any continual world model |
| 2 | `exp2_semiring_swap.py` | Same einsum + 3 semirings → 3 different computations. | T1 | ✅ | Boolean / shortest-path / reliability all from same loop | Confirms semiring is the meaning, einsum is the structure |
| 1 | `exp1_temperature_phase_transition.py` | There's a sharp phase transition between deductive (T=0) and analogical (T>0). | T1 | ❌ | smooth degradation, no transition; sigmoid floor=0.5 dominates | Floor problem named here → motivates exp9 (annealing) and exp29 (log-odds) |

## How to add a row

When you create `expN_*.py`:
1. Add the row at the top of the table (most recent first).
2. Fill **all** columns including "Learning → next". If you can't fill "Hypothesis" or "Learning → next", the experiment isn't worth running yet.
3. Keep "Key number" to one or two figures — the exact metric that decided the result.
4. After running, update Result and Key number with reality. **Do not edit the hypothesis after seeing the result** — that's how you delude yourself.

## Confirmed truths (so far)

- Tensor logic is a unifying primitive: einsum + nonlinearity covers KG inference, transitive closure, SSMs, and attention with the same shape.
- Symbolic composition is free and exact; bilinear embedding composition is near-random zero-shot.
- Generative replay (small GMM) beats weight regularization for continual learning at this scale.
- Semantic embeddings dramatically outperform random ones for relational learning (F1: 0.08 → 1.00).
- Semiring choice changes the meaning of the same computation, cleanly and without retraining.
- Adding SSM-style recurrent belief (`R̂[t+1] = obs + (1−obs)·forward(R̂[t], action)`) to a 16K-param forward model recovers object permanence under occlusion: in-occluder recall jumps 67% → 96-99% on the 8×8 world, stable across 20-step trajectories. The action-conditioned W slice IS the SSM's per-step A matrix; we don't need a separate recurrence mechanism. Memory is the active ingredient — both TL and MLP forward models benefit, and an equal/smaller-param MLP+memory matches or exceeds TL+memory under collision dynamics (exp40).
- TL's per-cell einsum prior gives **flatter accuracy degradation under distribution shift** than equal/larger MLPs, even though MLP wins absolute accuracy in every condition. Strongest TL-specific signal: under object-count transfer (train N=2, test N=3/4), TL holds 99.8% in-occluder recall while large MLPs (h=128/256) overfit to the 2-object input distribution and drop to 86-92%. Translation-equivariance is NOT free though — both TL (with absolute-position W) and MLPs collapse on occluder-position transfer. — exp41
- **TL has a sample-efficient advantage for object permanence** at low data: at n=50 trajectories, TL hits 95.5% in-occluder recall vs MLP's 91.1% (4.4pp gap, 2.5× lower variance). At n=2000 the gap inverts — MLP h=64 hits 99.9% vs TL's 97.9%. Crossover at n≈500. TL's overall P@2 stays flat at 88% regardless of data (structural ceiling); MLP scales from 90% → 93%. Classic bias-vs-capacity tradeoff. — exp42
- **TL with the right structural shape utterly destroys MLP on transitive closure**: a 3-scalar TL (`R ← σ(α·R@A + β·R + γ)` iterated K=4) at analytic init hits F1=1.000 zero-shot on the real tensor-project import graph from just n_train=20 random DAGs. A ~37k-param MLP (~12,400× more) tops out at F1=0.51 in-distribution and 0.38 on the same real graph at n_train=500. This works because the recurrence IS Boolean OR over matrix powers — the inductive bias is *exactly* the rule. Caveat: with neutral init (α=1, β=1, γ=-1), TL settles at a local minimum (F1=0.58 — still beats MLP, but not crushing). The structure is necessary; the init or some equivalent reparameterization is also needed. — exp44
- **TL's effective monoid is "thresholded sum-over-paths", broader than pure Boolean OR**: when trained for multipath2 (`≥2 distinct paths` target), the optimizer tunes α=3.6, γ=-3.8 so sigmoid saturates *only* when the path count is ≥2 — single-path cells get output ≈0.45 (below threshold), multipath cells get output ≈1.0. TL_OR F1=0.93 vs MLP F1=0.71. The expressive class is "≥k paths" for any threshold k, not just k=1. — exp45
- **TL fails decisively on tasks outside its monoid (parity / XOR / GF(2) arithmetic)**: target = `(# distinct paths) mod 2` collapses TL_OR to F1=0.26 — below the predict-all-positive baseline (~0.40 at 25% density), i.e., actively worse than chance. TL_LIN (no sigmoid) recovers to F1=0.52 — confirms sigmoid is the specific killer for XOR-shaped aggregation. MLP wins at F1=0.64. The structure-matching story holds: TL wins {OR, count-thresholds, monotone aggregation} and loses {XOR, parity, max/min, modular arithmetic}. — exp45
- **Non-monotonicity is necessary but not sufficient to break TL — combined with sparsity it is**: on `sd_geq_2` (reachable in ≥2 hops, non-monotone, ~13% density), TL_OR ties MLP at F1≈0.60. On `sd_geq_3` (≥3 hops, non-monotone, ~3% density), TL_OR collapses to F1=0.007 vs MLP's 0.18. The closure-shaped recurrence has enough expressivity to handle non-monotone + dense; sparsity removes the gradient signal needed to find the suppression operating point. — exp46
- **TL's iterative structural prior actively hurts trivial linear tasks**: predicting just A (the direct adjacency, no propagation needed) — TL_OR F1=0.13, MLP F1=0.57. TL_LIN F1=1.00 because β=2.05 drives linear passthrough — but TL_OR's forced sigmoid-after-iteration prevents this. The "structural commitment" of TL (mandatory recurrence) is useful when the task needs propagation, harmful when it doesn't. Implication: a "TL-or-not-TL" gating mechanism / depth-adaptive recurrence is the missing piece for tasks that span trivial → compositional. — exp46
- **TL's closure win scales for free; MLP collapses with size**: 3 TL params trained on n=16 random DAGs achieve **F1=1.000 zero-shot on n=128 graphs** (with K_eval=7 iterations). MLP re-trained from scratch at each size goes 0.53 → 0.20 → 0.01 → **0.00** as n grows from 16 to 128, even with 2.1M parameters at n=128. The 3 TL scalars encode the closure RULE (size-independent); MLP's n²-dim flat I/O cannot generalize across sizes and collapses under sparse-output BCE at high n. Real codebases have hundreds of modules — TL handles them with parameters trained on toy data. — exp47
- **TL substrate executes Tower of Hanoi where frontier LRMs collapse**: a state tensor `S[disk, peg]` plus a deterministic min-reduction legality check plus a tensor-update for moves, executes the recursive Hanoi algorithm with **100% legal moves and 100% goal-reached for every N from 3 to 20** (1,048,575 moves at N=20, 36.9s CPU). Apple's "Illusion of Thinking" reports o3-mini / DeepSeek-R1 / Claude-thinking collapsing to ~0% accuracy at N≈8-10 (where TL still runs in milliseconds). The Lawsen rebuttal attributes most of the LRM collapse to output-context truncation. Either way: this is a substrate-of-execution problem, not a reasoning problem. The algorithm is 6 lines; the work is maintaining typed state across exponentially many steps. **TL is the right substrate for problems with deterministic long-horizon execution where state must be maintained across many steps.** Complements the exp44/47/53 monotone-closure substrate story. — exp55
## Confirmed failures / dangers

- Sigmoid + threshold=0.5 makes "no evidence" look like 50% confidence; appears in exp1, exp5, exp6, exp9.
- T-annealing alone is not enough; needs paired baseline calibration.
- Naive prediction-error curiosity creates obsession hotspots at boundaries.
- Half-correct physics priors actively hurt — they bias the model in a wrong direction. All-or-nothing for explicit rules.
- TL's per-cell einsum factorization (`W[a, x, y, p, q]`) cannot represent dynamics with cross-cell AND-conditions like collision, where the transition for cell (x₁, y₁) depends on the state at cell (x₂, y₂). On collision-world + occluder + memory, TL plateaus at ~91% P@2 while an MLP at half the params hits ~96%. The inductive bias is structurally violated when the world has multi-object interactions. — exp40
- An absolute-position W tensor is NOT translation-equivariant: it parameterizes dynamics separately for each (x, y) source location. When the occluder zone moves to a region not seen during training, TL has no learned W for that region's hidden-object propagation — the model collapses to ~20% in-occluder recall (MLPs collapse harder, ~5%, but neither generalizes). To get spatial invariance, parameterize as `W[a, dx, dy]` (relative shift). — exp41
- TL's sharp predictions are incompatible with sampling-based MPC under cumulative-goal-prob reward: when most random action sequences score 0 (mass not at goal cell), argmax over zeros is essentially random → the planner can't exploit a sharp model. MLP's fuzzier predictions provide hill-climbing signal under the same planner. So TL's sample-efficient point-prediction advantage (exp42) doesn't translate to multi-step planning success on navigation: TL hits ~10% success across all data sizes, MLP h=128 hits 53-73%. To use TL with planning, need a different planner (BFS over thresholded next-states, distance-shaped reward) or a task that doesn't require multi-step rollouts. — exp43
- **Adding a Hadamard cross-term `(R@A)⊙R` to TL does NOT extend its expressive class to XOR/parity** — corrects the original exp48 mis-celebration. Frozen handcrafted XOR-init weights (α=4, β=4, γ_X=-8, δ=-2 — pre-sigmoid is XOR-shaped at iteration 1) score only F1=0.382 on parity, well below the celebrated trained F1=0.685. SGD drifts AWAY from XOR-init toward a closure-attenuated basin (γ_X: -8 → -4.1, β: 4 → 6.4). The trained F1=0.685 isn't XOR computation — it's the ceiling of "smart attenuated-OR". **The real barrier is sigmoid + iteration in real arithmetic**: σ(±2) = (0.12, 0.88) are non-Boolean and K=4 iterations smear them further. Sigmoid is not a homomorphism over GF(2). No choice of (α, β, γ_X, δ) lets real-valued sum-of-paths compose to mod-2 path-count. Adding parameters within sigmoid+iteration doesn't enlarge the expressive class — only changing the operator (cosine, complex-valued activation) or semiring (GF(2)) does. — exp48 (verified by `exp48_verify_xor.py`)

---

## External theory connection: parity barrier ↔ Tian's algebraic-decomposition framework

The exp48–50 results (parameter sweep, cosine activation, both fail on parity) have a clean mapping onto Yuandong Tian's CoGS framework (*Composing Global Solutions to Reasoning Tasks via Algebraic Objects in Neural Nets*, arXiv:2410.01779).

CoGS proves that two-layer nets with **quadratic activation** carry a *semi-ring algebraic structure* over weight space: gradient descent provably converges to solutions expressible as ring-algebraic compositions of partial solutions, and ~95% of GD runs on modular addition match the analytical construction. The result is *conditional on the activation admitting the right algebraic structure* — quadratic does, others may not.

The TL parity barrier is the empirical mirror of CoGS's positive case:

| | CoGS (positive) | TL exp48–50 (negative) |
|---|---|---|
| Substrate | 2-layer NN, quadratic activation | TL recurrence `R ← act(α·R@A + β·R + γ_X·(R@A)⊙R + δ)` |
| Algebraic structure on weight space | Semi-ring (sum/product compose) | Real-arithmetic sigmoid; not a homomorphism over GF(2) |
| Target task | Modular addition (decomposable in semi-ring) | Parity (requires GF(2), not decomposable in real-sigmoid algebra) |
| GD outcome | Converges to symbolic algebraic composition (~95%) | Converges to closure-attenuated basin, fails parity (F1≤0.69) |

**Restated finding under CoGS:** parity does not admit a decomposition compatible with TL's recurrence under sigmoid or cosine activations. exp48 (cross-term parameter) adds capacity within the same algebraic class without changing the class. exp50 (cosine activation) changes the activation but the period-2 structure interacts with the iterated-overwrite recurrence to destroy the path-count accumulator. Both are consistent with CoGS's prediction: the activation/substrate must admit the algebraic structure of the target task; otherwise GD has no path to a symbolic solution regardless of parameter count.

**Where this points the next experiments:**

1. Test TL with a **quadratic activation** (CoGS's positive case) on parity. Hypothesis: still fails — parity needs GF(2), not just *any* algebraic structure — but provides a same-recurrence control isolating activation from semiring.
2. Implement a **GF(2)-semiring TL** (binarized R, XOR composition) and verify: parity should be expressible by construction, demonstrating that the missing structure was the semiring, not the parameter count.
3. Map the **fragment-class / parity-class boundary** to CoGS-style algebraic conditions: which task families admit a decomposition compatible with TL's standard recurrence? This is the formal version of "TL is the limiting case where the algebraic structure is given as substrate, not discovered."

This is the language a CoGS-aware reviewer expects. The standalone framing ("TL fails at parity because sigmoid is non-monotone over GF(2)") is correct but provincial; the CoGS framing connects it to an active theory program that gives the negative result citational weight.
